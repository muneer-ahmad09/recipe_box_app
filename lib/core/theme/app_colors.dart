import 'package:flutter/material.dart';

class AppColors {
  // Surfaces
  static const paper = Color(0xFFF6F1E4);
  static const paperDim = Color(0xFFEFE7D2);
  static const cardWhite = Color(0xFFFFFDF7);

  // Text
  static const ink = Color(0xFF2A2118);
  static const inkFaint = Color(0xFF8A7F6C);
  static const headingMuted = Color(0xFF5F574C);

  // Brand
  static const petrol = Color(0xFF1F5C56);
  static const petrolDark = Color(0xFF15423D);

  // Accents
  static const mustard = Color(0xFFE3A72E);
  static const clayberry = Color(0xFFA6432B);

  // Borders / lines
  static const line = Color(0xFFD9CFB6);
}